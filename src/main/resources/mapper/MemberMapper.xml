<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.example.nowpt.mvc.mapper.MemberMapper">
        <select id="selectAllMember" resultType="MemberDto">
                SELECT
                    memb_sn as membSn,
                    memb_nm as membNm,
                    memb_id as membId,
                    use_yn as useYn,
                    email_addr as emailAddr
                FROM TB_MEMBER TM
        </select>

<!--        <update id="allNoticeN">-->
<!--                update TB_NOTICE set USE_YN = 'Y' where USE_YN = 'N'-->
<!--        </update>-->
</mapper>