import React from 'react';
import styled from "styled-components";

const CalendarDayDetailComponent = (data) => {
    console.log(data);


    return (
        <div>

        </div>
    );
};

const CalendarDayDetailWrap = styled.div`
    
`

export default CalendarDayDetailComponent;